function calc(x: number) -> number 
{ 
    var i = 10
    var count = 0

    while(i > 0) 
    {
        if(count == x) 
        {
            continue
        }

        count = count + 1    
        i = i - 1

        if(count > x)
        {
            break
        }
    }

    return count
}